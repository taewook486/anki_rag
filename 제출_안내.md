# 기말 제출본 실행 안내 — Anki RAG

작성자: 황태욱
제출일: 2026-06-13
대상: 평가자(교수)

본 문서는 압축 패키지를 받으신 교수님께서 즉시 시연·평가 가능하도록 작성된 실행 가이드입니다.

---

## 1. 패키지 구성

```
anki_rag/
├── src/                # 핵심 모듈 (parser, embedder, retriever, rag, agent, adaptive, graph, ...)
├── tests/              # pytest 167개 (수집 기준)
├── data/               # 학습 데이터 (.apkg 5개 + 10000.txt + media + graph)
├── qdrant_data/        # 사전 빌드된 Qdrant 인덱스 (73,410 포인트)
├── pyproject.toml      # 의존성 정의
├── README.md           # 프로젝트 개요·아키텍처·데모 응답 샘플
├── CHANGELOG.md        # 버전 이력
├── start.bat           # Windows 일괄 실행 스크립트
├── .env.example        # 환경변수 템플릿 (API 키 비어있음)
└── 제출_안내.md       # 본 문서
```

설계 문서(HWP)는 별도 제출되었으며 본 압축본에는 포함되지 않았습니다.

---

## 2. 설치 전 주의사항 (한국어 Windows)

압축 해제 위치 경로에 **한글이 포함되어 있으면** Python venv 초기화 단계에서 다음과 같은 오류가 발생합니다.

```
Fatal Python error: init_import_site: Failed to import the site module
UnicodeDecodeError: 'cp949' codec can't decode byte 0xec ...
```

원인: 한국어 Windows의 OS 기본 인코딩(cp949)이 venv 내부의 UTF-8로 저장된 한글 경로를 디코딩하지 못해서 발생합니다. 다음 중 **하나**를 적용해 주십시오.

### 해결 A (권장) — 영문 경로에 압축 해제

`C:\anki_rag\` 또는 `D:\anki_rag\` 처럼 **경로 전체가 영문**인 곳에 압축을 푸시면 됩니다.

```powershell
# 예: D:\anki_rag 로 이동
cd D:\
# 압축 파일을 D:\ 에 둔 상태에서
Expand-Archive -Path "anki_rag_submit.zip" -DestinationPath "anki_rag_submit"
cd anki_rag_submit\anki_rag
```

### 해결 B (대안) — UTF-8 모드 환경변수

이미 한글 경로에 풀어두셨다면 모든 Python 명령 실행 전에 환경변수를 설정하시면 됩니다.

```powershell
# PowerShell
$env:PYTHONUTF8=1

# CMD
set PYTHONUTF8=1
```

이 설정은 현재 터미널 세션에서만 유효하므로 새 터미널을 열 때마다 한 번씩 설정하시거나, 시스템 환경변수로 영구 등록하시면 됩니다.

영구 등록 (PowerShell 관리자):
```powershell
[Environment]::SetEnvironmentVariable("PYTHONUTF8", "1", "User")
```

이미 venv를 만든 상태에서 위 에러를 만나셨다면 venv를 재생성하시는 게 가장 안전합니다.

```powershell
$env:PYTHONUTF8=1
Remove-Item -Recurse -Force .venv
python -m venv .venv
.venv\Scripts\activate
pip install -e .
```

---

## 3. 빠른 실행 (3 단계)

### 단계 1. 의존성 설치

```bash
# Python 3.13 가상환경 생성
python -m venv .venv

# 활성화
# Windows
.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

# 패키지 설치
pip install -e .
```

설치 항목 중 BGE-M3 모델은 **첫 검색 시 자동 다운로드**됩니다 (약 2 GB, ~5 분).
설치 시점이 아니라 첫 실행 시점에 받습니다.

### 단계 2. WordNet 코퍼스 다운로드 (GraphRAG 활성화)

```bash
python -m nltk.downloader wordnet omw-1.4
```

### 단계 3. LLM API 키 설정

`.env.example` 을 `.env` 로 복사 후 키 입력:

```bash
# Windows
copy .env.example .env

# macOS/Linux
cp .env.example .env
```

`.env` 를 텍스트 편집기로 열어 `ANTHROPIC_API_KEY` 값을 실제 키로 교체.

Anthropic 키가 없으시면 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` 세 줄을
주석 해제하고 OpenAI 호환 API(GLM, OpenRouter 등)를 사용하실 수 있습니다.

---

## 4. 실행

### 옵션 A — Web UI (권장, 시각적 시연용)

```bash
# Windows 한 번에 실행
start.bat

# 또는 수동 실행 (두 터미널)
uvicorn src.api.main:app --host 127.0.0.1 --port 8000
streamlit run src/web/app.py
```

브라우저에서 http://127.0.0.1:8501 접속 → 좌측 사이드바에 4개 탭:
- **🔍 검색** — 단어 검색 + 덱 필터 + 오디오 재생
- **💬 채팅** — RAG 기반 대화
- **🕸️ 지식 그래프** — WordNet 단어 관계 시각화 (Plotly)
- **⚙️ 관리** — 인덱싱 상태 / 캐시 통계 / 서버 정보

### 옵션 B — CLI (단순 시연용)

```bash
# 단일 질의
python -m src query "abandon의 뜻과 예문을 알려줘"

# 대화형 모드 (반복 입출력 루프, Ctrl+C로 종료)
python -m src chat --stream

# 단순 검색 (LLM 호출 없음)
python -m src search "abandon" --top-k 5
```

### 옵션 C — REST API 직접 호출

```bash
uvicorn src.api.main:app --host 127.0.0.1 --port 8000
```

OpenAPI 문서: http://127.0.0.1:8000/docs

주요 엔드포인트:
- `POST /api/query` — 일반 RAG 질의
- `POST /api/adaptive` — 쿼리 복잡도 자동 분류 (Simple/Moderate/Complex)
- `POST /api/agent` — ReAct Agent 직접 호출
- `GET /api/graph/related/{word}` — 단어 관계 조회
- `GET /api/graph/stats` — 그래프 통계

---

## 5. 첫 호출 시 주의 (콜드 스타트)

BGE-M3 모델이 첫 검색 호출 때 디스크에서 메모리로 로드되어 **첫 응답까지 약 30~45초** 소요됩니다. 두 번째 호출부터는 1~2초 내 응답합니다.

평가 시연 직전에 다음 명령으로 사전 워밍업하시면 콜드 스타트를 피할 수 있습니다:

```bash
curl -X POST http://127.0.0.1:8000/api/query \
  -H "Content-Type: application/json" \
  -d "{\"question\":\"abandon meaning\"}"
```

---

## 6. 시연 시나리오 5종 (각 응답에 출처·복잡도·전략 메타데이터 포함)

각 시나리오는 두 가지 방식으로 호출할 수 있습니다.

- **방법 1 — Streamlit UI** (가장 쉬움): http://127.0.0.1:8501 의 "💬 채팅" 탭에 질문 그대로 입력.
  단 채팅 탭은 내부적으로 `/api/query` (단순 RAG) 를 호출하므로 Adaptive 분기·GraphRAG Fusion 효과는
  메타데이터로 확인할 수 없습니다. 메타데이터(`complexity`, `strategy_used`, `graph_used`, `agent_steps`) 가
  필요하면 아래 방법 2 사용.
- **방법 2 — PowerShell + curl.exe** (메타데이터 확인용): 한국어 입력은 PowerShell 의 BOM 문제를
  피하기 위해 **`Out-File -Encoding utf8NoBOM`** 로 임시 JSON 파일을 만들고 `--data-binary "@파일명"` 으로 전달.

### A. Simple 쿼리 (Dense only)

```powershell
'{"question":"abandon meaning","use_graph":false}' | Out-File -Encoding utf8NoBOM -FilePath body_A.json
curl.exe -X POST http://127.0.0.1:8000/api/adaptive -H "Content-Type: application/json" --data-binary "@body_A.json"
```

기대 메타데이터:
- `complexity`: `simple`
- `strategy_used`: `dense_only`
- `graph_used`: `false`
- `sources`: 2~3 건 (abandon, abandonment 등)

### B. Moderate 쿼리 (Hybrid RRF)

```powershell
'{"question":"give up과 유사한 구동사","use_graph":true}' | Out-File -Encoding utf8NoBOM -FilePath body_B.json
curl.exe -X POST http://127.0.0.1:8000/api/adaptive -H "Content-Type: application/json" --data-binary "@body_B.json"
```

기대 메타데이터:
- `complexity`: `moderate`
- `strategy_used`: `hybrid_rrf`
- `graph_used`: `false` (구현상 GraphRAG Fusion 은 Complex 경로에서만 자동 활성화. Moderate 는 Hybrid RRF 까지)
- `sources`: 3 건 (give up¹/³/⁵ — 동일 단어의 다른 의미)

### C. Complex 쿼리 (Agent ReAct + GraphRAG Fusion)

```powershell
'{"question":"비즈니스에서 계약 해지 관련 단어를 난이도 순으로 정리해줘","use_graph":true}' | Out-File -Encoding utf8NoBOM -FilePath body_C.json
curl.exe -X POST http://127.0.0.1:8000/api/adaptive -H "Content-Type: application/json" --data-binary "@body_C.json"
```

기대 메타데이터:
- `complexity`: `complex`
- `strategy_used`: `agent_react`
- `graph_used`: `true`
- `total_agent_steps`: 3
- `agent_steps`: 각 스텝의 `thought`/`tool`/`args`/`observation` 노출
- `sources`: `[]` (Agent 경로는 출처를 `agent_steps[].observation` 안에 텍스트로 노출)

### D. 할루시네이션 방어

```powershell
'{"question":"quibblesnark의 뜻은?","top_k":3}' | Out-File -Encoding utf8NoBOM -FilePath body_D.json
curl.exe -X POST http://127.0.0.1:8000/api/query -H "Content-Type: application/json" --data-binary "@body_D.json"
```

기대 응답:
- `answer` 본문에 `"검색된 자료에 없습니다"` 명시
- `sources`: `[]` (자료 없음 — 정상)
- 응답 본문에 유사 단어 `quibble` 또는 철자 재확인 안내가 추가될 수 있음 (LLM 자율 판단)

### E. 출처 추적

A~D 응답의 `sources` 필드는 모두 다음 3-필드 스키마를 일관 노출합니다.

```json
[{"word": "abandon", "source": "--forvo-youglish_link", "deck": "해커스-초록이"}]
```

- `word`: 검색된 단어 또는 표현
- `source`: 원본 .apkg 파일명
- `deck`: Anki 덱 이름 (한국어)

C 시나리오의 경우 `sources` 가 비어있는 대신 `agent_steps[].observation` 안에 `(toefl_voca_v1)` 처럼 출처가 텍스트로 인용됩니다.

> **시연 팁**: 시나리오 B 의 GraphRAG 효과(SYNONYM 인접 노드 자동 보강)를 시각적으로 보여주려면
> Streamlit "🕸️ 지식 그래프" 탭에서 `abandon` 예시 버튼을 누르세요. SYNONYM 4건
> (desert/desolate/forsake/vacate) 이 방사형으로 시각화됩니다.

---

## 7. 테스트

```bash
# 전체 회귀 (167개)
pytest

# 빠른 모듈별
pytest tests/test_graph.py tests/test_adaptive.py tests/test_agent.py -v
```

---

## 8. 문제 해결

| 증상 | 원인 | 해결 |
|------|------|------|
| `UnicodeDecodeError: 'cp949' codec can't decode ...` | 압축 해제 경로에 한글 포함 | 챕터 2 해결 A 또는 B 적용 |
| `ModuleNotFoundError: No module named 'src'` | 가상환경 미활성화 또는 pip install -e . 미실행 | 챕터 3 단계 1 재실행 |
| `ANTHROPIC_API_KEY ... ValueError` | `.env` 미설정 | 챕터 3 단계 3 재실행 |
| `Storage folder ... already accessed` | CLI 와 API 서버가 동시에 Qdrant 점유 | 한쪽만 실행 |
| 첫 응답 30~45초 지연 | BGE-M3 콜드 스타트 (정상) | 챕터 5 워밍업 적용 |
| Streamlit "🕸️ 지식 그래프" 탭 빈 화면 | 그래프 파일 없음 | `python -m src index` 1회 재실행 |

---

## 9. 핵심 기술 스택

- Python 3.13
- BGE-M3 (BAAI/bge-m3) — Dense 1024d + Sparse SPLADE 동시 임베딩
- Qdrant — 로컬 파일 모드 + Named Vector
- NetworkX + NLTK WordNet — 지식 그래프
- FastAPI + Streamlit + Click
- Claude Sonnet 4.6 (Anthropic) / OpenAI 호환 LLM 폴백

자세한 설계 의도와 구현 결과는 별도 제출된 HWP 문서를 참고해 주십시오.
